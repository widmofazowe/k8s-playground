apiVersion: apps/v1
kind: Deployment
metadata:
  name: den
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      name: web
  template:
    metadata:
      labels:
        name: web
    spec:
      containers:
      - name: den
        image: gecko:latest
        imagePullPolicy: Never